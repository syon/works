box: node

build:
  steps:
    - npm-install
    - script:
        name: Nuxt.js Generate
        code: |
          npm run generate

deploy:
  steps:
    - lukevivier/gh-pages:
        token: $GH_TOKEN
        basedir: dist
